import Layout from "@/components/layout/Layout";
import ReferenceHome from "@/components/sonar/ReferenceHome";

/** Página inicial reconstruída a partir da direção visual aprovada. */
const HomePage = () => (
  <Layout hideWhatsApp>
    <ReferenceHome />
  </Layout>
);

export default HomePage;
